<% if user.avatar.attached? %>
    <%= link_to (image_tag user.avatar_thumbnail, id: 'avatar') , user_info_path(user)%>
<% else %>
    <%= link_to (image_tag 'avatar2.jpg', id:'avatar'), user_info_path(user) %>
<% end %>