<% if active_relation?(user) %>
    <%= button_to "unfollow ", remove_path(user),remote: true, method: :delete %>
<% else %>
    <%= button_to "follow ", add_path(user),remote: true, method: :post %>
<% end %> 