<% if following_topic?(topic) %>
    <%= button_to "Unfollow", topic_unfollow_path(topic) , method: :delete, remote: true %> 
<% else %>
    <%= button_to "Follow", topic_follow_path(topic) , method: :post , remote: true%> 
<% end %>