<table>
    <h1>List of Answers</h1>
    <% question.answers.each do |list| %>
        <tr>
        <th><%= list.answer %></th>
        <td>By: <%= User.find(list.user_id).name %></td>
        <td><%= time_ago_in_words(list.created_at) %> ago</td>
        </tr>
    <% end%>
</table>